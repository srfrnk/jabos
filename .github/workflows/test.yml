name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |-
          echo start
      - uses: ./.github/workflows/my-action
        id: hello
      - run: echo "${{ steps.hello.outputs.what }}"
      - run: |-
          echo end
